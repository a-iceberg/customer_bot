version: '3.8'

services:
  customer_bot_vlad_test:
    container_name: customer_bot_vlad_test
    network_mode: host
    restart: unless-stopped
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - TZ=Europe/Moscow
      - LANGCHAIN_TRACING_V2=true
      - LANGCHAIN_ENDPOINT=https://api.smith.langchain.com
      - LANGCHAIN_PROJECT=customer_bot_vlad_test
    volumes:
      - ./data:/app/data
    command: ["/bin/sh", "-c", "if [ ! -f /app/data/auth.json ]; then echo '{\"LANGCHAIN_API_KEY\": \"\", \"OPENAI_API_KEY\": \"\", \"ANTHROPIC_API_KEY\": \"\", \"1С_TOKEN\": \"\", \"1C_LOGIN\": \"\", \"1C_PASSWORD\": \"\"}' > /app/data/auth.json; fi && exec gunicorn 'bot:app' --workers 1 --worker-class 'uvicorn.workers.UvicornWorker' --bind '0.0.0.0:7406' --timeout 1800"]